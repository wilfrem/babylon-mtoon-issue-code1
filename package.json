{
  "name": "test-mtoon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=dist/bundle.js --sourcemap",
    "start": "http-server dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kazuki Yasufuku",
  "license": "MIT",
  "dependencies": {
    "@babylonjs/core": "^5.17.1",
    "babylon-mtoon-material": "^4.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.51",
    "http-server": "^14.1.1"
  }
}
